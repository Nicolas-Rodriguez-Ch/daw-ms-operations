spring:
  application:
    name: ms-operations
eureka:
  client:
    service-url:
      defaultZone: ${eureka_url:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true
    hostname: ms-operations

vehicles-service:
  url: http://localhost:8081/vehicles/%s

server:
  port: 8082